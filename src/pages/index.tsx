import { Inter } from "next/font/google";

const inter = Inter({ subsets: ["latin"] });

export default function Home() {
  return (
    <div>
      <h1> this is a test with a new push with preview hiiii</h1>
    </div>
  );
}
